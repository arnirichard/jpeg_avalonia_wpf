<UserControl x:Class="DAW.PitchDetector.PitchDetectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DAW.PitchDetector"
             xmlns:signalplot="clr-namespace:SignalPlot;assembly=SignalPlot"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">

            <StackPanel Orientation="Vertical" Margin="50,0">
                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock Text="{Binding File.Name}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock Margin="5,0" x:Name="numberOfSamples" HorizontalAlignment="Left" VerticalAlignment="Top" />
                    <TextBlock Margin="5,0" x:Name="totalDuration" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock Margin="5,0" x:Name="sampleRateText" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock Margin="5,0" x:Name="bitsPerSampleText" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock Margin="5,0" x:Name="ChannelsText" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                </WrapPanel>
                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="{Binding ElementName=signalPlot, Path=IndexFrom, StringFormat='Range {0}'}"/>
                    <TextBlock Text="{Binding ElementName=signalPlot, Path=IndexTo, StringFormat='-{0}'}"/>
                    <TextBlock Text="{Binding ElementName=signalPlot, Path=FromX, StringFormat='Time {0:0.000}'}" Margin="10,0,0,0"/>
                    <TextBlock Text="{Binding ElementName=signalPlot, Path=ToX, StringFormat='-{0:0.000}s'}"/>
                </WrapPanel>
                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="{Binding ElementName=signalPlot, Path=CurrentIndex, StringFormat='Sample {0}'}"/>
                    <TextBlock Margin="10,0"
                               Text="{Binding ElementName=signalPlot, Path=CurrentX, StringFormat='Time {0:0.000}s'}"/>
                    <TextBlock Margin="10,0"
                               Text="{Binding ElementName=signalPlot, Path=CurrentValue, StringFormat='Value {0}'}"/>
                    <TextBlock Margin="10,0"
                               Text="{Binding ElementName=pitchPlot, Path=CurrentValue, StringFormat='Pitch {0:0.00}Hz'}"/>
                    <TextBlock Margin="10,0" x:Name="periodTextBlock"/>
                </WrapPanel>
                <TextBlock Text="{Binding ElementName=signalPlot, Path=SelectedIntervalLength, StringFormat='Selected interval {0} samples'}"/>
            </StackPanel>


        </StackPanel>

        <signalplot:Plot x:Name="signalPlot" Grid.Row="1" UnitX="ms" DataContext="{Binding SignalPlotData}"/>

        <signalplot:Plot x:Name="pitchPlot" Grid.Row="2" UnitX="ms" UnitY="Hz" DataContext="{Binding PitchPlotData}"
                         IndexFrom="{Binding ElementName=signalPlot, Path=IndexFrom, Mode=TwoWay}"
                         IndexTo="{Binding ElementName=signalPlot, Path=IndexTo, Mode=TwoWay}"
                         CurrentIndex="{Binding ElementName=signalPlot, Path=CurrentIndex, Mode=TwoWay}"/>

    </Grid>
</UserControl>
